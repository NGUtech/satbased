modules:
  config:
    \Helper\Api:
      url: '%APP_SCHEME%://%APP_HOST%'
      elasticsearch:
        url: '%APP_SCHEME%://%APP_HOST%:9200'
        username: elastic
        password: '%ELASTIC_PASSWORD%'
      cookies:
        domain: '.%APP_HOST%'
        jwt: dev-_jwt
        xsrf: dev-_xsrf