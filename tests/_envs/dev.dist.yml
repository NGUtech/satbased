modules:
  config:
    \Helper\Api:
      url: '%APP_SCHEME%://%APP_HOST%'
      elasticsearch:
        url: http://localhost:9200
        username: elastic
        password: '%ELASTIC_PASSWORD%'
      cookies:
        domain: '.%APP_DOMAIN%'
        jwt: dev-_jwt
        xsrf: dev-_xsrf